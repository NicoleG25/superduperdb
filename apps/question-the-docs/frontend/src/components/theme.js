// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light', // Initial theme mode
        primary: {
            main: '#007FFF', // Your primary color
        },
        // ... other palette options
    },
});

export default theme;
